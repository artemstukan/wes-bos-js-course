export const lastName = 'stukan';
export function returnHi(name) {
        return `hi ${name} ${lastName}`;
}

console.log('we are in utils.js');

const first = 'Artem is my first name';
export default first;
